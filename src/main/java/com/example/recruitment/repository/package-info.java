/**
 * Spring Data JPA repositories for the entities in the domain package.
 * Owns all direct persistence access; nothing outside this package (and the
 * service layer that calls it) should talk to the database directly.
 */
package com.example.recruitment.repository;
