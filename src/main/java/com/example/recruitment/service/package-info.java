/**
 * Application services. Orchestrate a use case (e.g. "create a processing job")
 * by coordinating the parser, matching, repository and kafka packages. Holds
 * workflow/orchestration logic, not domain rules and not infrastructure detail.
 */
package com.example.recruitment.service;
