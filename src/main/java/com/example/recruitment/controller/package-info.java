/**
 * HTTP entry points (Spring MVC controllers). Translates HTTP requests into
 * calls on the service layer and translates results back into HTTP responses.
 * Must not contain business logic, persistence logic, or Kafka logic.
 */
package com.example.recruitment.controller;
