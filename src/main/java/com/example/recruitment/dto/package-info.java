/**
 * Data transfer objects: request/response shapes exposed at the HTTP
 * boundary and Kafka message payloads. Kept separate from domain entities so
 * that persistence structure and wire format can evolve independently.
 */
package com.example.recruitment.dto;
